version: "3.3"

services:
  wordpress:
    image: {{ wordpress_image }}
    ports:
      - "{{ wordpress_port }}:80"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: {{ mysql_image }}
    env_file:
    - .env
